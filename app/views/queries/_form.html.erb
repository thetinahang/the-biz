<%= form_for @query do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <h3>Query Terms:</h3>
  <%= f.fields_for :terms do |term| %>
    <%= term.collection_select :name, Term::TYPES, :to_s, :to_s %>
    <%= term.text_field :value %><br>
  <% end %>

  <%= f.submit %>
<% end %>
