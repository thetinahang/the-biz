<%= form_for @query do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.fields_for :terms do |term| %>
    <%= term.label :name %>
    <%= term.text_field :name %><br>
  <% end %>

  <%= f.submit %>
<% end %>
